## Tests
 - `exposure`
 - `tone_map_reinhard`

## `Color4`-Specific Operations (Alpha)

### Premultiplied Alpha  
*(Linear + floatable only)*
- `premultiply()`
- `unpremultiply()`

### Compositing  
*(Linear + floatable only)*
- `over(background)`
- `under(foreground)`