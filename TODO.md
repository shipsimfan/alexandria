# ToDo
 1. Finish math library
    1. Matrix4x4
    2. Quaternion
    3. Color4
 2. Add input system
    1. Add easy interface for keyboard to `StateTrackingInput`
    2. Add calling of `frame`
    3. Add extended bits to scan code
    4. Don't send repeated key press events
    5. Change `StateTrackingInput` to track if a release or press happens in a frame
    6. Add `Display` impl for `KeyCode`
 3. Add cube example with movement
 4. Add instance rendering
 5. Add textures

## Post Colosseum
 6. Add mouse support
 7. Add xbox controller support
 8. Add ACSL

## Later TODO
 1. Add MSAA
 2. Add post-processing
 3. Add adjustable render scale
 4. Add toggleable wireframe rendering
 5. Add joystick support
 6. Add steering wheel support
 7. Add other controller support